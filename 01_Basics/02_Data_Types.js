"use strict" // treat all js code as newer version of js

// alert(3+3) will not work we are using node or deno not a browser

let name = "manya"
let age = 18
let isLoggedIn = false

// null is a stand alone value 
// undefinedis different from null // null is empty while undefined is value not assigned
// symbol //this datatype is used for finding uniqueness
// object 
// we can use typeof to find the datatype of something

console.log(typeof undefined) //the type will be undefined
console.log(typeof null) // the type will be object 
